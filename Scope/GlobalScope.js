// 6. Global scope
// The global scope is the outermost scope. It is accessible from any inner scope.

//  the topmost scope of JavaScript file loaded using <script> tag is a global scope:

<script src="myScript.js"></script>

// myScript.js
// "global" scope
let counter = 1;

// A variable declared inside the global scope is named global variable. 
// Global variables are accessible from any scope

